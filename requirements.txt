# requirements.txt — зависимости для локального запуска (Python 3.10+)

# --- UI / API ---
streamlit>=1.32
requests>=2.31
aiohttp>=3.9

# --- Core data / retrieval ---
numpy>=1.26,<3
pandas>=2.1
scikit-learn>=1.3
chromadb>=0.4.22,<1

# --- Embeddings / LLM tooling (используется в app/engine.py и app/search_tools.py) ---
torch>=2.2
transformers>=4.41,<4.50
sentence-transformers>=2.6
sentencepiece>=0.1.99
accelerate>=0.27
llama-index-core~=0.10.0
llama-index-vector-stores-chroma~=0.1.0
llama-index-embeddings-huggingface
llama-index-readers-file

# --- Parsing / документы ---
PyMuPDF>=1.24
pdfplumber>=0.10
python-docx>=1.1
python-pptx>=0.6.23
docx2txt>=0.8
python-slugify>=8.0

# --- Russian NLP / segmentation ---
natasha>=1.6
razdel>=0.5
sumy>=0.11

# Optional: пакет simhash можно не ставить — в коде есть fallback
# simhash>=2.1.2
